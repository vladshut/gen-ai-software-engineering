# Required: secret key for /admin/stats endpoint.
# Generate a strong random value, e.g. `python -c "import secrets; print(secrets.token_urlsafe(32))"`
ADMIN_API_KEY=replace-me

# Optional: SQLite database path (default: tasks.db)
# TASKS_DB_PATH=tasks.db
